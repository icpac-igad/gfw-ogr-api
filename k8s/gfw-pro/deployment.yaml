apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: gfw-ogr-gfw-pro
  name: gfw-ogr-gfw-pro
spec:
  revisionHistoryLimit: 2
  template:
    metadata:
      labels:
        name: gfw-ogr-gfw-pro
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: type
                operator: In
                values:
                - gfw-pro
      containers:
      - name: gfw-ogr-gfw-pro
        image: vizzuality/gfw-ogr-gfw-pro
        imagePullPolicy: Always
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
        args:
          - start
        env:
          - name: PORT
            value: "3200"
          - name: NODE_ENV
            value: prod
          - name: NODE_PATH
            value: app/src
          - name: LOCAL_URL
            value: http://gfw-ogr-gfw-pro.default.svc.cluster.local:3200
          - name: CT_URL
            valueFrom:
                secretKeyRef:
                  name: mssecrets
                  key: CT_URL
          - name: CT_TOKEN
            valueFrom:
                secretKeyRef:
                  name: mssecrets
                  key: CT_TOKEN
          - name: CT_REGISTER_MODE
            valueFrom:
                secretKeyRef:
                  name: mssecrets
                  key: CT_REGISTER_MODE
          - name: API_VERSION
            valueFrom:
                secretKeyRef:
                  name: mssecrets
                  key: API_VERSION

        ports:
          - containerPort: 3200

      restartPolicy: Always
